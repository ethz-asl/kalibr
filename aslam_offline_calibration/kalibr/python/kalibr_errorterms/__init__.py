import aslam_cv_backend #needed for errorterm base class wrapper

# NB: We import these bindings from the global path as we have to
# hack the installation due to multiple Python packages in one
# catkin package.
from libkalibr_errorterms_python import * 
